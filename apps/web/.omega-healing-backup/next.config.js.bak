/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,

  experimental: {
    esmExternals: 'loose'
  },

  webpack: (config) => {
    config.resolve.alias = {
      ...(config.resolve.alias || {})
    }

    return config
  }
}

module.exports = nextConfig
